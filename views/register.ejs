<!DOCTYPE html>
<html lang="es">
  <%- include('./partials/head') %> <!-- Incluimos el contenido del head -->

  <body>
    <!-- Header -->
    <% if (locals.user != undefined) { %>
      <%- include('./partials/headerLogueado') %>
    <% } else { %>
      <%- include('./partials/header') %>
    <% } %>
    <!-- /Header -->

    <!-- Novedades -->
    <div class="container products-wrapper">
      <div class="d-flex justify-content-center align-items-center min-vh-100">
        <form class="formLOG w-100" action="/users/register" method="POST">
          <div class="title">Registrate</div>

          <div class="input-container ic1">
            <input id="name" class="form-input" type="text" name="nombre" placeholder="Nombre" required />
          </div>

          <div class="input-container ic1">
            <input id="email" class="form-input" type="email" name="email" placeholder="Email" required />
          </div>

          <div class="input-container ic1">
            <input id="password" class="form-input" type="password" name="contrasenia" placeholder="Contraseña" required />
          </div>

          <!-- Botón para enviar el formulario -->
          <button type="submit" class="btn w-100">Crear</button>
        </form>
      </div>
    </div>

    <!-- Footer -->
    <%- include('./partials/footer') %>
    <!-- /Footer -->
  </body>
</html>
